# Commit on Each Achievement

- Create a commit after each completed implementation achievement (e.g., a passing TDD cycle, a new feature unit, a refactor step).
- Each commit must leave the codebase in a passing state: all tests green, lint clean.
- Keep commits small and atomic — one logical change per commit.
- Do not batch unrelated changes into a single commit.
